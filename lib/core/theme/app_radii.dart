/// Corner radii ported from src/styles.css (--radius: 1rem base).
class AppRadii {
  AppRadii._();

  static const sm = 12.0;
  static const md = 14.0;
  static const lg = 16.0;
  static const xl = 20.0;

  /// Matches AppCard's rounded-3xl in the web app's mobile UI kit.
  static const card = 24.0;
}
